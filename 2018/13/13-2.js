/*
*/

const {assert} = require("chai");
const input = require("./13-input.js");

describe("Examples", () => {

  it("", () => {

  });

});

xdescribe("Puzzle Solution", () => {

  const answer = null;
  it(answer.toString(), () => {
    assert.isNotNull(answer);
  });

});
